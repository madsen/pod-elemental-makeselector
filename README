Pod-Elemental-MakeSelector version {{$version}}, released {{$date}}


This module provides an alternative to Pod::Elemental::Selectors.  It
allows more complex criteria to be expressed by a single selector,
including criteria that Pod::Elemental::Selectors doesn't currently
handle.



INSTALLATION

To install this module, run the following commands:

{{ $t->build_instructions }}



DEPENDENCIES

{{ $t->dependency_list }}



CHANGES
    Here's what's new in version {{$version}} of Pod-Elemental-MakeSelector:
    (See the file "Changes" for the full revision history.)

{{$changes}}



COPYRIGHT AND LICENSE

{{ local $_ = $zilla->license->notice; chomp; $_ }}
